# Conventions

## Coding Style
- Follow PEP 8.
- Use `ruff` for linting and formatting.
- Maintain strict typing with `pyright`.

## CLI Design
- Use `argparse` for CLI argument parsing.
- Provide clear help messages and error handling.
