# Agent Instructions: Restriction

AI Agents are FORBIDDEN from editing, modifying, or adding any files in this directory (`src/cli/`).

This folder contains stable CLI tools that should only be modified by human developers or under explicit human supervision via a different process.
